{
  "mcpServers": {
    "windows-dev-agent": {
      "command": "python",
      "args": ["-m", "src.claude_server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "WINDOWS_DEV_AGENT_DATA_DIR": "${CLAUDE_PLUGIN_DATA}",
        "WINDOWS_DEV_AGENT_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      },
      "description": "Claude Code adapter for the shared Windows Dev Agent runtime. Exposes environment inspection, capability routing, workflow planning, package setup, isolation, ecosystem inventory, and audit tools."
    }
  }
}
