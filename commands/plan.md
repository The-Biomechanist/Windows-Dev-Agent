---
description: Build a structured execution plan for a consequential or multi-step Windows development task.
---

$ARGUMENTS is the task description.

Use the bundled `windows-dev-agent:workflow-plan` skill as the canonical procedure. Preserve `$ARGUMENTS` as the requested task and do not duplicate the skill's routing, authority, dependency, rollback, or verification rules in this Claude command adapter.
