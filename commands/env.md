---
description: Inspect the Windows development environment and relevant surrounding agent/tool configuration.
---

Use the bundled `env-inspect` skill as the canonical procedure for this command.

If the user also asks about the surrounding agent/tool ecosystem, use `ecosystem_scan` only for the surfaces it actually establishes. Do not duplicate the skill's evidence, freshness, or package-routing rules in this Claude command adapter.
