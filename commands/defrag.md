---
description: Inventory an existing agent/developer-tool setup, identify overlap, and plan a reversible consolidation.
---

Use the bundled `windows-dev-agent:ecosystem-defrag` skill as the canonical procedure for this command.

Treat any `$ARGUMENTS` as additional scope or constraints. Do not duplicate or weaken the skill's inventory, overlap, authority, rollback, or verification rules in the command adapter.
